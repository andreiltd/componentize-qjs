package local:test;

interface math {
    add: func(a: s32, b: s32) -> s32;
    multiply: func(a: s32, b: s32) -> s32;
}

world imports {
    import math;
    
    // Export that uses the import
    export double-add: func(a: s32, b: s32) -> s32;
}
